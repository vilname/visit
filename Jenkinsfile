pipeline {
    agent any
    options {
        timestamps()
    }
    stages {
        stage("Down") {
            steps {
                sh "make docker-down-clear"
            }
        }
    }
    post {
        always {
            sh "make docker-down-clear || true"
        }
    }
}
